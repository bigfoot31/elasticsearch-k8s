filebeat:
  inputs:
  - type: log
    enabled: true
    paths:
    - /usr/share/filebeat/logs/*.log
    fields:
      node: ${NODE_NAME}
    fields_under_root: true

output:
  logstash:
    hosts: "logstash-svc.elkstack.svc.cluster.local:5044"
